/**
* HashFunction is an interface for classes that provide hashing algorithms to implement.
* 
* @author       Toby Flynn
*/
public interface HashFunction {
    
    public String hashPassword(String password);
    
}
